-- cart-service holds cart state in-memory (session-cookie-keyed) per its ownership contract.
-- This migration exists to satisfy schema provisioning; no tables are required for current functionality.
SELECT 1;
