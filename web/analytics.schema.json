{
  "events": [
    {
      "name": "page_viewed",
      "description": "Fired on every page load.",
      "properties": {
        "path": "string — window.location.pathname of the loaded page"
      }
    },
    {
      "name": "landing_cta_clicked",
      "description": "Fired when the landing page's primary CTA is clicked, navigating to the product listing page.",
      "properties": {
        "destination": "string — target page identifier, 'listing'"
      }
    }
  ]
}
