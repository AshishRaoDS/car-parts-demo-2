{
  "events": [
    {
      "name": "page_viewed",
      "description": "Fired on every page load.",
      "properties": { "page": "string" }
    },
    {
      "name": "cta_clicked",
      "description": "Fired when the landing page CTA is clicked to navigate to the listing page.",
      "properties": { "page": "string", "target": "string" }
    }
  ]
}
